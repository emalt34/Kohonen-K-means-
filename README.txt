README

This program runs an unsupervised learning algorithm that separates unclassified data into two clusters using Kohonen and K-means methods.
It requires the python packages numpy, csv, and random. 
